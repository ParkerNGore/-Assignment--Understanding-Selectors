Why are ID selectors a bad idea?
They cannot be reysed. They also can cause specificity issues.

What selectors should you prefer when working with CSS?
Class selectors. This way you can effect a specific h1 element instead of all h1 elements